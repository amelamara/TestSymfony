name: phpunit
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-versions: ['7.1']
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: composer install
      - name: PHPUnit
        run: php bin/phpunit
